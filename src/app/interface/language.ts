export interface Language {
  about: string;
  whatIDo: string;
  skills: string;
  projects: string;
  contact: string;
  changeLanguage: string;
}

export interface Translations {
  pl: Language;
  en: Language;
}
